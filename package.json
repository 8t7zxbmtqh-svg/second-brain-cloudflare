{
  "name": "assistente-ia-second-brain-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Assistente IA personale per Cloudflare Workers + Assets + D1.",
  "scripts": {
    "check": "node --check worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
